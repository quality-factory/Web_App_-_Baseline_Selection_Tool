# Auto detect text files and perform LF normalization
* text=auto

# Add language-specific line ending and binary rules after instantiation.
# Example for PowerShell:
#   *.ps1 text eol=crlf
#   *.psm1 text eol=crlf
# Example for binary output formats:
#   *.wfw binary
